## diagram
```mermaid
{{
- Translate every user request into **valid Mermaid code** for rendering in Obsidian; wrap any text with special characters in straight quotes and insert manual line‑breaks every ~20 characters for narrow diagrams.
- Support and recognise all core Mermaid chart types (flow, sequence, Gantt, class, pie, state, ER) and apply grouping where it aids readability; never add colour or styling until the user explicitly approves.
}}
```